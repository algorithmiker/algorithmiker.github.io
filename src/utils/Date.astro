---
export interface Props {
 	date: Date,
}

const { date } = Astro.props;
function zeroPad(i: number) {
	return i < 10 ? `0${i}` : i.toString();
}
---
<time datetime={date.toDateString()}>{zeroPad(date.getFullYear())}-{zeroPad(date.getMonth()+1)}-{zeroPad(date.getDate())}</time>

